#include "main.h"

/**
 * _sqrt_recursion - calculate square root
 * @n: intger input
 * Return: square root
 */
int _sqrt_recursion(int n)
{
	int sqrt;

	sqrt = 1;
	if ((sqrt * sqrt) != n)
		sqrt = sqrt * _sqrt_recursi:wq
			on();
}
